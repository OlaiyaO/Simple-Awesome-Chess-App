import 'package:flutter/material.dart';

class PlayComputerScreen extends StatelessWidget {
  static const String routName = "Play Computer";

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
